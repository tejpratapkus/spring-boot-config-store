spring:
  datasource:
    url: jdbc:mysql://localhost:3306/app_customer?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: welcome
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
   # properties:
   #  hibernate:
   #   dialect: org.hibernate.dialect.MySQL55Dialect
  application:
    name: customer
